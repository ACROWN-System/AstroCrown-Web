# AstroCrown Web Tests

Reusable visual and functional tests for AstroCrown Web.

## Structure

- `COLOR-PALETTE/` — color-system tests
  - `TEXT/` — text-color tests
    - `WARM-NEAR-WHITE/` — warm near-white text comparison

Tests should be created once, preserved, and reused rather than recreated for the same design question.
